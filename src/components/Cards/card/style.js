import styled from 'styled-components'

const Image = styled.img`
  width: 100%;
  height: auto;
  display: block;
`

const Card = styled.div`
  width: 500px;
`
export const Styled = {
    Card,
    Image
}
