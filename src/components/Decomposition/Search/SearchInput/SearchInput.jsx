import React from 'react';

/**
 *
 * @param params
 * @returns {JSX.Element}
 * @constructor
 */
const SearchInput = (params) => {
    return (
        <input {...params}/>
    );
};

export default SearchInput;